{
  "hook_event_name": "SessionStart",
  "matcher": "startup",
  "session_id": "test-session-016",
  "cwd": "/tmp",
  "_test": {
    "hook": "boot",
    "args": ["startup"],
    "expected_exit": 0,
    "comment": "Health-checks brokers; exits 0 even if brokers are down (just emits additionalContext)"
  }
}
