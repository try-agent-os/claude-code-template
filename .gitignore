# --- AgentOS repo-sync contract: runtime data and credentials never commit ---
data/
.env
.env.*
.aop/
.claude/
*.pem
*.key
*credential*
*secret*
.session-state.json

# Managed-repo clones (declared in .agentos/workspace.yaml)
workspace/*/
!workspace/.gitkeep

# OS noise
.DS_Store
Thumbs.db
