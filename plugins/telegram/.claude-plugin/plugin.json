{
  "name": "telegram",
  "version": "vendored-3f6171e",
  "description": "Telegram bot interface via MTProto. Channel push for incoming messages, tools for replies and bot management.",
  "category": "agentos-core",
  "mcpServers": "./mcp.json",
  "userConfig": {
    "bot_token": {
      "type": "string",
      "sensitive": true,
      "description": "Telegram bot API token from @BotFather"
    },
    "admin_user_ids": {
      "type": "string",
      "description": "Comma-separated Telegram user IDs of authorized admins (empty = no access). Auto-populated by the install.sh / Mac wizard via /start polling."
    },
    "admin_usernames": {
      "type": "string",
      "description": "Comma-separated usernames (display only, populated by setup wizard)."
    }
  }
}
