{
  "mcpServers": {
    "telegram": {
      "command": "node",
      "args": ["${CLAUDE_PLUGIN_ROOT}/dist/index.js"],
      "env": {
        "TELEGRAM_BOT_TOKEN": "${userConfig.bot_token}",
        "TELEGRAM_USER_ID": "${userConfig.user_id}",
        "PORT": "3848"
      }
    }
  }
}
