{
  "name": "claude-peers",
  "version": "vendored-fc26491",
  "description": "Inter-agent messaging via shared broker daemon. Exposes both tool calls (send_message, list_peers, set_summary) and channel push for active receivers.",
  "category": "agentos-core",
  "mcpServers": "./mcp.json",
  "userConfig": {
    "broker_db_path": {
      "type": "string",
      "default": "${CLAUDE_PLUGIN_DATA}/claude-peers.db",
      "description": "Path to shared SQLite broker database"
    }
  }
}
