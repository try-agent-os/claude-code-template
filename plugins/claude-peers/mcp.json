{
  "mcpServers": {
    "claude-peers": {
      "command": "bun",
      "args": ["${CLAUDE_PLUGIN_ROOT}/server.ts"],
      "env": {
        "CLAUDE_PEERS_DB": "${CLAUDE_PLUGIN_DATA}/claude-peers.db"
      }
    }
  }
}
