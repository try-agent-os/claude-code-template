# Agents

Sub-agent definitions and prompts — reviewers, researchers, domain
specialists the operator can delegate to. One directory or markdown file per
agent.

Empty is fine to start with: the root `CLAUDE.md` operator handles everything
until a task is narrow and recurring enough to warrant its own specialist.
