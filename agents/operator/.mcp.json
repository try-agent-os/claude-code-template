{
  "_comment": "MCP servers used by the operator agent. Replace ports/URLs if your setup differs. telegram-mcp + saga-mcp must be running before operator boots.",
  "mcpServers": {
    "telegram": {
      "type": "sse",
      "url": "http://localhost:3848/sse"
    },
    "saga-mcp": {
      "type": "sse",
      "url": "http://localhost:3851/sse"
    }
  }
}
