{
  "$comment": "AgentOS managed settings — installed to /etc/claude-code/managed-settings.json by install.sh. Schema validated against claude-code 2.1.119+. Note: the managed-settings allowlist is consulted but channels also gate on a separate built-in allowlist that we haven't fully reverse-engineered yet — see saga-mcp task #802 for the open issue.",

  "channelsEnabled": true,
  "minimumVersion": "2.1.119",

  "allowedChannelPlugins": [
    {"plugin": "claude-peers", "marketplace": "agentos"}
  ],

  "enabledPlugins": {
    "claude-peers@agentos": true,
    "agentos-core@agentos": true,
    "template-dev@agentos": true,
    "agent-sdk-dev@agentos": true,
    "code-review@agentos": true,
    "commit-commands@agentos": true,
    "security-guidance@agentos": true
  }
}
